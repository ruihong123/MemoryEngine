#
# Non-Replication Configuration
# Format matches connection_cloudlab_replica.conf:
# Line 1: Compute nodes
# Line 2: Memory nodes
# Lines 3+: logical_id size primary [replicas...]
# In this version every logical region has exactly one primary (no replicas).
#
node-0 node-1
node-17 node-9

# logical_id  size     primary
1             16g       0
3             16g       1

